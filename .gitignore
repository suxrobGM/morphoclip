# Byte-compiled
*.py[cod]
*$py.class
__pycache__/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# uv / venv
.venv/
venv/
env/

# Project
outputs/
checkpoints/
*.pt
*.pth
*.ckpt

# Data (keep structure via .gitkeep)
# `data/**/` excludes directories outright, so git never descends into them and a bare
# `!data/<dir>/` negation cannot re-include anything. Un-ignoring a subtree needs both the
# directory and its contents, as `data/reference/` does below. The platemap tree the tests
# need is committed under tests/fixtures/ instead, so it lives with the tests that read it.
data/**/
data/.cache/
!data/**/.gitkeep
!data/features/.gitkeep
!data/tensors/.gitkeep
!data/visualizations/**

# First-party reference metadata (small, required by the benchmark harness)
!data/reference/
!data/reference/**

# Environment / secrets
.env
.env.*
!.env.example

# IDE / OS
.DS_Store
.idea/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# Build / dist
dist/
build/
*.egg-info/
*.egg

# Logs / coverage
*.log
.coverage
.coverage.*
coverage.xml
coverage.json
htmlcov/

.claude/settings.local.json
output/
.VSCodeCounter/

# LaTeX build files
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.lof
*.lot
*.out
*.toc
*.synctex.gz
*.synctex(busy)
*.nav
*.snm
*.vrb
*.dvi
*.ps
