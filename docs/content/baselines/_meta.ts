import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  cellclip: "CellCLIP",
  "benchmark-guide": "Benchmark Guide",
  "benchmark-reproducibility": "Benchmark Results",
  "baseline-results": "Baseline Results",
};

export default meta;
